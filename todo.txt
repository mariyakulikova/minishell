/* Mariia */
!!! echo hello"world$USERS$SHLVL" test - should delete $USERS, but still change $SHLVL
!!! echo helloworld$'USERS'$SHLVL - should remove $'USERS', but still process $SHLVL
!!! echo "''helo''""hey"
$? + $?
echo test >666
heredoc in parant process
heredoc unlink temp files
envp
/* Mariia */

/* Fernanda */
create a function to clean before reprompting
- fix built in
see how to update **envp
exit status update
signals
$SHLVL when ./minishell inside of minishell

PRompt:
echo test>666
Return:
minishell(74323,0x205f94c00) malloc: Double free of object 0x13fe08300
minishell(74323,0x205f94c00) malloc: *** set a breakpoint in malloc_error_break to debug
zsh: abort      ./minishell

prompt:
echo '> >> < * ? [ ] | ; [ ] || && ( ) & # $  <<' 
echo 'exit_code ->$? user ->$USER home -> $HOME'
mini output = ()
bash output = (exit_code ->$? user ->$USER home -> $HOME)
mini exit code = 139
bash exit code = 0

